<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".gui.mainScreen.cardCreator.SelectBundleActivity">

    <ScrollView
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:id="@+id/scrollView3"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true">

    </ScrollView>

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/selectBundle_listV"
        android:choiceMode="singleChoice"
        android:listSelector="@color/colorSelectedButton"
        android:layout_alignBottom="@+id/scrollView3"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/selectBundle_textView_title" />

    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="Large Text"
        android:id="@+id/selectBundle_textView_title"
        android:layout_alignParentTop="true"
        android:layout_alignEnd="@+id/scrollView3" />

</RelativeLayout>
